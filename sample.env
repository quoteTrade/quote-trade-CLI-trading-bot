API_BASE_URL=https://app.quote.trade/api
LIQUIDITY_WS_URL=wss://app.quote.trade/ws/liquidity
# Trade balances come from this WS (ACCOUNT_UPDATE). app.quote.trade: wss://app.quote.trade/ws/listenKey
LISTEN_KEY_WS_URL=wss://app.quote.trade/ws/listenKey

# ─────────────────────────────────────────────────────────────────────────────
# Auth Mode A: SHA256 API keys (users registered via UI)
# Copy TRADE_API_KEY and TRADE_API_SECRET from the platform UI after registration.
# ─────────────────────────────────────────────────────────────────────────────
TRADE_API_KEY=
TRADE_API_SECRET=

MODE=real

# ─────────────────────────────────────────────────────────────────────────────
# Auth Mode B: Existing wallet (register via CLI using your own private key)
# The bot derives your wallet address, calls getChallenge → sign → register → logon.
# After successful registration the SHA256 API keys are printed — save them above.
# Use with: npm run cli -- register --existing-wallet
# ─────────────────────────────────────────────────────────────────────────────
# WALLET_PRIVATE_KEY=0x<your-ethereum-private-key>

# ─────────────────────────────────────────────────────────────────────────────
# Auth Mode C: New wallet (register via CLI with a freshly generated wallet)
# No private key needed — the bot generates one and prints it for you to save.
# Use with: npm run cli -- register --create-wallet
# ─────────────────────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────────────────────
# Optional: Ed25519 public key to attach during registration (any wallet mode).
# Can be a PEM string, a base64 DER blob, or a path to a PEM file.
# If omitted, no Ed25519 key is attached.
# Alternatively pass --ed25519-public-key on the CLI.
# ─────────────────────────────────────────────────────────────────────────────
# ED25519_PUBLIC_KEY=-----BEGIN PUBLIC KEY----- MCow... -----END PUBLIC KEY-----

# ─────────────────────────────────────────────────────────────────────────────
# Signing algorithm: sha256 (default) or ed25519
# Controls how the bot signs API request headers (signature + X-Mbx-Apikey).
#
# Both modes use the SAME TRADE_API_KEY and TRADE_API_SECRET env vars.
# SIGNING_ALGORITHM controls how TRADE_API_SECRET is interpreted:
#
#  sha256  (default) — HMAC-SHA256(TRADE_API_SECRET, payload) → hex
#                      TRADE_API_SECRET is an HMAC key string
#                      X-Mbx-Apikey = TRADE_API_KEY
#
#  ed25519           — Ed25519.sign(TRADE_API_SECRET, payload) → base64
#                      TRADE_API_SECRET is the BASE64 BODY of the PKCS#8 private key
#                      (no -----BEGIN/END PRIVATE KEY----- headers — just the middle part)
#                      X-Mbx-Apikey = TRADE_API_KEY
#
# To use Ed25519 signing:
#   1. Register with --generate-ed25519-key (prints Ed25519 API key + private key base64)
#   2. Set TRADE_API_KEY    = Ed25519 API key (printed as KEY SET 2)
#   3. Set TRADE_API_SECRET = base64 body of the private key (printed as KEY SET 2, no headers)
#   4. Set SIGNING_ALGORITHM = ed25519
#
# Override per-run with: --signing-algorithm sha256|ed25519
# ─────────────────────────────────────────────────────────────────────────────
# SIGNING_ALGORITHM=sha256

# ─────────────────────────────────────────────────────────────────────────────
# Deposit (ERC-20): network and contract addresses for USDC/USDT
# ─────────────────────────────────────────────────────────────────────────────
# DEPOSIT_NETWORK=mainnet   # mainnet | sepolia | polygon | amoy
# DEPOSIT_RPC_URL=https://eth.merkle.io
# USDC_CONTRACT_ADDRESS=
# USDT_CONTRACT_ADDRESS=
